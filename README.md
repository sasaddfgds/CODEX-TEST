# ImGui "привет" example

Небольшая программа на C++ с Dear ImGui, которая выводит слово `привет`:
- в консоль (`std::cout`),
- в окне ImGui.

## Сборка

```bash
cmake -S . -B build
cmake --build build
```

## Запуск

```bash
./build/imgui_privet
```
